using Microsoft.Xna.Framework;
using Microsoft.Xna.Framework.Graphics;
using System;
using System.Collections.Generic;
using System.Drawing;
using System.Linq;
using System.Numerics;
using System.Text;
using System.Threading.Tasks;

namespace PassarosPasseando.Content
{
    public class SheerHeartAttack
    {
        /*
        #region VARIABLES
        Texture2D image;
        Vector2 position, speed;
        Point size;
        Color color;
        #endregion
        
        Game game;

        public SheerHeartAttack(Game game, Texture2D image, Vector2 position, Vector2 speed, Point size, Color color)
        {
            this.game = game;
            this.image = image;
            this.position = position;
            this.speed = speed;
            this.size = size;
            this.color = color;
        }
        */
    }
}
